texto = "Decir cuántas veces aparece cada una de las 3 letras dentro del texto. Importante: convertir todo a minúsculas para que no importe si el usuario escribió mayúsculas o minúsculas."
letras = "a, b, c"
texto_min = texto.lower()
print(texto.count(letras))